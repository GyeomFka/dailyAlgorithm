Abstract 라는 객체지향 개념에서 주목해야할 특징은

Abstract로 지정된 Method or Class는 직접적으로 사용할 수가 없고, 반드시 그것을 상속할 클래스를 만들어서
상속한 클래스를 사용하도록 강조하도록

* 상속해서 사용하도록 강제 하는것 *

why ?

약간 인터페이스랑 비슷한개념이네 ? - 인터페이스와는 다른점

abstract class {
    abstract a ();
    abstract b ();
    void c (){
        c로직은 공통 분모
    }
}